.container
	.row
		.col-md-5.col-md-offset-1
			%ul
				- User.all.each do |u|	
					- next if u == current_user
					%li
						= u.email
						\&nbsp;
						= button_to 'Add User to Friends', friends_path(u), method: :post
